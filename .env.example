# Optional: override API (defaults to localhost in dev, Render URL in production build)
# VITE_API_URL=https://finance-dashboard-backend-2fn9.onrender.com/api

# Optional: show a fixed “live app” label (login page uses window.location.origin by default)
# VITE_LIVE_FRONTEND_URL=https://your-site.netlify.app
